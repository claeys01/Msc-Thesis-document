% \resizebox{\linewidth}{!}{%

% 
  \begin{tikzpicture}[
        % ===================================================================
        % STYLES — edit here to restyle the whole flowchart
        % ===================================================================
        node distance      = 6mm and 11mm,
        font               = \scriptsize,
        % --- node shapes ---
        startstop/.style   = {ellipse, draw, thick, align=center, fill=gray!12,
                              minimum height=7mm,  minimum width=14mm},    
        %                              
        cfdproc/.style     = {rectangle, draw, thick, align=center,
                              minimum height=8mm,  minimum width=30mm, fill=blue!20},
        %
        mlproc/.style      = {rectangle, draw, thick, align=center,
                              minimum height=8mm,  minimum width=30mm, fill=gray!20},
        %
        trainproc/.style   = {rectangle, draw, thick, align=center,
                              minimum height=8mm,  minimum width=30mm, fill=green!20},
        %
        retrainproc/.style = {rectangle, draw, thick, rounded corners=2.5pt, align=center,
                              minimum height=8mm,  minimum width=30mm, fill=purple!15},
        %
        decision/.style    = {diamond, draw, thick, aspect=2.6, inner sep=1pt, align=center,
                              fill=yellow!22},
        %
        flag/.style        = {rectangle, draw, thick, rounded corners=2.5pt, align=center,
                              minimum height=7mm,  minimum width=30mm, fill=red!14},
        % --- arrows / labels ---
        flow/.style        = {-{Latex[length=2mm,width=1.6mm]}, thick},
        edgelab/.style     = {font=\tiny, inner sep=1.5pt, fill=white},
    ]

    % ===================================================================
    % PHASE 1 — initial training (top, vertical)
    % ===================================================================
    \node[startstop]                         (start)        {Start};
    \node[cfdproc,   right=of start]         (warmup)       {Warmup: pure CFD\\$0 \le t^* < t^*_\text{train}$, collect $\mathbf{u}$ and $\boldsymbol{\mu}_0$};
    \node[trainproc, above right=of warmup]  (train)        {Train $\varphi_\theta$, $\psi_\theta$, $f_\phi$};
    
    \node[cfdproc, right=of warmup]          (continue)     {WaterLily continues while pipeline trains}; 

    \node[mlproc, right=of continue]         (hybrid)       {Hyrid rollout-cfd phase};

    \end{tikzpicture}
% }